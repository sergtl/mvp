// Kept only for the raw <select> elements below: there is no Select
// primitive in components/ui/ yet, so these style themselves directly
// (Input/Textarea bring their own styling instead).
export const control = "w-full min-w-0 rounded-md border bg-background p-2 text-sm";
